# Preferences

Chosen, not observed. Asked once, changed when the operator changes their mind. Delete a line rather than guessing at it — an unanswered preference is better than an invented one.

## Tooling

- **Implementation language for controllers:** _(e.g. TypeScript run with `bun`)_
- **Installer of choice:** _(e.g. homebrew · official script · manual · ask each time)_
- **Install policy:** _`ask` offers and waits · `never` reports the gap and stops · `auto` installs without asking, and is honoured only in delivery mode_
- **Shell:**

## Working style

- **How much explanation is wanted**, and where more is wanted than usual.
- **What must be confirmed before it happens** — pushing, opening a pull request, anything outward-facing.
- **Standing authorizations**, so they are not re-requested every session.

## Harness

- **Harness in use, and whether that is a constraint or a choice.** The difference decides whether suggesting a different one is help or noise.
